# Canonical sources

A registry of facts that exist in more than one place in this codebase by necessity — a value list, a field order, an enum — mapped to the ONE file that owns each and everywhere else that reads it. Check this before adding a new consumer of a shared fact, and before deciding a fact needs a canonical source it doesn't have yet.

**Why this file exists.** Two 3.15 bugs and one 3.16-era fix shared one root cause: a second, independent copy of a list that already existed elsewhere silently fell behind the original when a value was added to one and not the other, and nothing caught it because nothing checked the two against each other. The rule that follows from that — [CONTRIBUTING.md's Code Style](../../.github/CONTRIBUTING.md#code-style) — says to derive from a canonical source instead of retyping one. This file is what makes that checkable: it names where each canonical source actually is, so "does this already exist somewhere" is a lookup, not a grep-and-hope.

| Fact | Canonical source | Consumers | Kept in sync by |
| --- | --- | --- | --- |
| Per-provider price/spec field membership + emit order | `scripts/lib/record-schema.js` (`FIELD_ORDER`, `specFields()`, `priceFields()`) | `fetch-vantage.js`, `split-data.js`, `data-diff.js`, `reconcile-data.js` | `data-integrity-test.js`, `split-data-test.js`, `record-schema-test.js`, `data-diff-test.js`, `reconcile-data-test.js` |
| Recognised ENV / OS / Workload / Compliance values (upload hygiene) | `src/core/rules/rule-engine.js` (`RuleEngine.RECOGNIZED`) | `src/features/ingest.js` (hygiene check), `src/features/xlsx-export.js` (template) | Read live from `RuleEngine.RECOGNIZED` at call time, never copied. `RECOGNIZED.workload` is AWS's family-key list, and `tests/suites/engine/workload-vocabulary-parity-test.js` pins all three providers' `WORKLOAD_FAMILIES` key sets identical — a mismatch fails CI rather than silently falling through to a provider's "general" list. |
| Which Compliance option applies to which provider (Current-Gen: all three; Nitro: AWS-only; Confidential: all three since v3.16.16's GCP series match; Trusted Launch: Azure-only) | `src/core/rules/rule-engine.js`'s `apply()` — the `requiresAwsNitro`/`requiresConfidential`/`requiresTrustedLaunch` gate conditions (Rule 1b) | Each page's `ruleDefaultCompliance*` checkbox markup (`aws.html`/`azure.html`/`gcp.html`/`multicloud.html` only show the options that gate has an effect on there); `tests/suites/infra/page-parity-test.js`'s `EXPECTED` map | **Not code-derived** — a static HTML page can't read a JS gate condition at load time, so the checkbox markup and the test's `EXPECTED` map are both hand-copies of the same fact, distinct from the rule gates themselves. `page-parity-test.js` catches drift BETWEEN the 4 pages' markup and its own `EXPECTED` map, but not drift between either of those and the rule gates — check `apply()`'s Rule 1b by hand when a Compliance option's provider applicability changes. |
| User-defined-rule dimensions + actions | `src/core/rules/user-rules.js` (`USER_RULE_DIMENSIONS`/`USER_RULE_ACTIONS`, via `userRuleDimensionOptions()`/`userRuleActionOptions()`) | `src/ui/user-rules-ui.js` (the panel's two dropdowns) | `tests/suites/ui/user-rules-ui-test.js`. Fixed 2026-09-03 — the UI used to hand-list both sets independently. |
| What counts as a Windows OS row | `src/core/rules/rule-engine.js` (`RuleEngine.isWindowsOS`) | `src/core/rules/rule-engine.js`'s own ARM exclusion, `src/core/engine/base-instance-selector.js`'s `_poolForOS` | Read live from `RuleEngine.isWindowsOS` at call time. `_poolForOS` keeps a same-shaped local fallback ONLY for `os-aware-pricing-test.js`, which loads it without RuleEngine on purpose — `windows-os-matching-test.js` pins the two call sites agree. Fixed 2026-09-04 — the ARM exclusion used an EXACT match against two bare tokens while `_poolForOS` used a PREFIX match, so a real string like "Windows Server 2022" satisfied one and not the other. |
| Column auto-mapping synonyms | `src/shared/app-core.js` (`COLUMN_SYNONYMS`) | `src/features/ingest.js` | Single real consumer; low risk, listed for completeness. |
| Per-provider region availability | The shipped manifest (`{P}_REGION_KEYS` in `src/providers/{p}/{p}-data.js`) | The hardcoded `awsRegions` list (`src/providers/aws/aws-instance-selector.js`) and Azure's display-name map (`src/providers/azure/azure-instance-selector.js`), which feed manual-entry autocomplete | **Not code-derived, deliberately** — manifest keys aren't human-friendly for a dropdown, so this is a genuine second list, not a hand-copy mistake. `tests/suites/infra/lazy-test.js`'s "every manifest region is offered for autocomplete" check is what actually keeps the two in agreement — if you add a region, update the hardcoded list too, or that test tells you. |
| The set of alternative-strategy column names ("Most Cost Optimized" / "Workload Based" / "Newest Generation" / "Best Network") | `src/core/engine/instance-selector-factory.js` — the `result[\`${providerUpper} <name>\`] = formatAlternative(...)` assignments, where the column names first exist | `src/features/preview.js`'s `altSuffixes`, `src/features/xlsx-export.js`'s `STRATEGY_SHEET_NAMES`, `scripts/data/recommendation-diff.js`'s `PICK_SUFFIXES` (a superset — also covers "Instance"/"Custom Fit") | **Not code-derived** — each is a separately hand-typed list of the same 4 strings, for a different purpose (grouping in the preview grid, generating one Excel sheet per strategy, classifying which columns count as a "pick" for flip detection). Adding v3.16.18's `Best Network` required touching all 4; nothing catches a future one going missing from any single list — add it to all 4 by hand, or a strategy silently doesn't get grouped/sheeted/diffed. |

## Adding an entry

A fact belongs here when it's read by more than one file and retyping it would be a plausible mistake — not every constant in the codebase, only ones where a second copy could silently exist. When you introduce a canonical source (or find you're about to duplicate one that should have been), add a row: the fact in plain words, the file and export that owns it, every real consumer, and whatever actually keeps them honest — a shared import, a live read, or a test that checks the two sides agree when the values genuinely can't be unified (like the region-availability row above).
