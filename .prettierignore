# Vendored third-party builds — must stay byte-identical to upstream so they
# can be diffed against the published artifact. Formatting them would also
# rewrite the minified bundles into something unreviewable.
js/vendor/

# Generated by tools/split-data.js from the provider data dumps; regenerate
# rather than hand-format.
js/aws/regions/
js/azure/regions/
js/gcp/regions/

# Committed test fixtures, compared byte-for-byte.
tests/golden/goldens/

node_modules/
